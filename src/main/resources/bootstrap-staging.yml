server:
  port: 6383
spring:
  profiles: staging
  cloud:
    config:
      server:
        native:
          # write yml file route
          search-locations: file:///${user.home}/Desktop/server-configs
