server:
  port: 6383
spring:
  profiles:
    # CONFIG_ENV: git or native
    active: ${CONFIG_ENV:git}
  application:
    name: config-server
---
spring:
  profiles: native
  cloud:
    config:
      server:        
        native:
          search-locations: file:/config
---
spring:
  profiles: git  
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/virnect-corp/PF-Configurations"
          username: VIRNECT-Owner
          password: f4f37c34c276080e0b3832792c1ceb0d2ac9f3c5
          searchPaths: develop,staging,production,onpremise,freezing
          force-pull: true
